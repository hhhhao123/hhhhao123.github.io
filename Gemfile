source "https://rubygems.org"

# Jekyll 4 is compatible with newer Ruby versions used for local builds.
gem "jekyll", "~> 4.4"

# Required by Jekyll and its dependencies when running on newer Ruby versions.
gem "csv"
gem "bigdecimal"
gem "webrick"
